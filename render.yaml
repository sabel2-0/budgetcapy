services:
  - type: web
    name: budget-tracker
    runtime: node
    startCommand: npm start
    buildCommand: npm install
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
